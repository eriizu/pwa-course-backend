# Use postgres/example user/password credentials
version: "3.1"

services:
  db:
    image: postgres:alpine
    restart: always
    ports:
      - 5432:5432
    env_file: conf.env
    # environment:
    #   POSTGRES_PASSWORD: pwa
    #   POSTGRES_USER: pwa
  # adminer:
  #   image: adminer
  #   restart: always
  #   ports:
  #     - 8080:8080
